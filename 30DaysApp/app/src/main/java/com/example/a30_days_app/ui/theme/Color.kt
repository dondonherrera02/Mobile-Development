package com.example.a30_days_app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Light Theme Colors ────────────────────────────────────────────────────────

/** Primary brand color – deep indigo */
val PrimaryLight = Color(0xFF3D5AFE)

/** Lighter shade used on primary containers */
val PrimaryContainerLight = Color(0xFFDDE1FF)

/** On-primary text/icon color */
val OnPrimaryLight = Color(0xFFFFFFFF)

/** Secondary accent – teal */
val SecondaryLight = Color(0xFF00BCD4)

/** Secondary container */
val SecondaryContainerLight = Color(0xFFCFF5F9)

/** Background for the full screen */
val BackgroundLight = Color(0xFFF8F9FF)

/** Surface color for cards */
val SurfaceLight = Color(0xFFFFFFFF)

/** Text on surfaces */
val OnSurfaceLight = Color(0xFF1A1C2E)

/** Subtle label / secondary text */
val OnSurfaceVariantLight = Color(0xFF44476A)

// ─── Dark Theme Colors ─────────────────────────────────────────────────────────

/** Primary brand color – bright indigo for dark theme */
val PrimaryDark = Color(0xFF8C9EFF)

/** Primary container in dark mode */
val PrimaryContainerDark = Color(0xFF1A2980)

/** On-primary dark */
val OnPrimaryDark = Color(0xFF00105C)

/** Secondary accent dark */
val SecondaryDark = Color(0xFF80DEEA)

/** Secondary container dark */
val SecondaryContainerDark = Color(0xFF004D58)

/** Dark background */
val BackgroundDark = Color(0xFF0F1020)

/** Dark surface for cards */
val SurfaceDark = Color(0xFF1C1E30)

/** Text on dark surfaces */
val OnSurfaceDark = Color(0xFFE4E5F0)

/** Subtle label on dark */
val OnSurfaceVariantDark = Color(0xFFB0B3CC)